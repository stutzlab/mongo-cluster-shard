version: '3.5'

services:

  shard1-a:
    build: .
    image: stutzlab/mongo-cluster-shard
    environment:
      - SHARD_REPLICA_SET=shard1
      - SHARD_SERVER_NODES=shard1-a,shard1-b
    ports:
      - 27511:27017

  shard1-b:
    build: .
    image: stutzlab/mongo-cluster-shard
    environment:
      - SHARD_REPLICA_SET=shard1
      - SHARD_SERVER_NODES=shard1-a,shard1-b
    ports:
      - 27512:27017
